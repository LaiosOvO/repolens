# 代码仓库教学与复用索引：产品简报

> 文档状态：需求与研究结论，不是详细技术设计。

## 一句话产品定义

给定一个本地代码仓库，先生成一份有重点、可追证据的功能讲解 HTML；用户选中想复用的功能后，再导出一套可直接交给 Agent 使用的 Skill 和源码索引。

## 30 秒结论

- **不是再做一个自动 Wiki**：自动 Wiki 擅长覆盖，但覆盖不等于让人做出复用决策。
- **先回答人的问题**：这个项目值得看吗、最重要的功能是什么、一次真实请求如何穿过源码、哪些部分值得复用。
- **再回答 Agent 的问题**：为实现选中的功能，按什么顺序读哪些模块、文件和符号，去哪里找测试，哪些许可证和风险不能越过。
- **页面必须总—分—总**：结论和建议在前，源码证据按需展开，最后回到明确的“复用 / 参考 / 不采用”决定。

## 为什么现有 HTML 不好用

以当前 Claude Agent SDK Python 页面为例，主要问题不是配色，而是信息模型错误：

1. README 的标题被直接当成功能，甚至把 Release Workflow、Types 也提升成和产品能力同级。
2. 每一项都套用相同的“入口层、运行层、界面层”句式，无法说明该功能独有的实现机制。
3. 文件清单很多，但没有一个真实请求的入口、状态变化、关键分支和结果。
4. “值得复用什么”和“不要复用什么”只有一句泛化结论，不能支持选型。
5. 人读页面与 Agent 读源码混在同一层，结果两边都不够好用。

## 目标读者与读完后的动作

### 人的决策模式

读者可能不熟悉仓库语言和目录。读完后必须能做四个动作：

1. 判断这个项目是否值得继续投入时间。
2. 选择要深入或复用的功能。
3. 看懂选中功能的一条实现主线。
4. 知道应整段复用、只借鉴设计，还是放弃。

### Agent 的执行模式

Agent 不需要再通读整份研究报告。它收到选中功能的导出包后，应能：

1. 确认目标仓库、固定提交和许可边界。
2. 按阅读顺序定位入口、核心符号、数据/控制流和测试。
3. 区分“必须读”“按需读”“禁止直接复制”的代码。
4. 在修改后运行规定的验证，并发现索引是否已经过期。

## 默认 HTML 的信息结构

### 1. 首屏：先给决定

首屏只回答：

- 项目用一句话是什么；
- 对当前产品的价值等级；
- 最值得借鉴的 1–3 项；
- 最大限制或风险；
- 推荐动作：复用、参考、观察或不采用。

### 2. 产品能力地图

只列用户可感知或架构上真正独立的能力，不照抄 README 目录。每项能力都要标明成熟度和证据强度。

### 3. 一条黄金路径

选择最能代表项目价值的真实场景，用 5–8 个步骤讲清楚：请求从哪里进入、谁编排、状态存在哪里、外部工具如何调用、如何返回结果、哪些测试锁定行为。

### 4. 功能卡

每个功能统一回答以下问题，但不使用重复套话：

| 问题 | 页面必须给出的答案 |
|---|---|
| 用户得到什么 | 可观察的产品行为，不是类名 |
| 为什么重要 | 它解决的真实场景 |
| 核心机制 | 一句话解释独有的实现方式 |
| 实现主线 | 入口 → 编排/核心 → 状态/外部依赖 → 输出 |
| 源码证据 | 关键文件、符号、测试；可点击展开 |
| 复用建议 | 直接复用、改造复用、只参考或不采用 |
| 风险边界 | 许可证、沙箱、服务绑定、未实现部分、运行验证状态 |

### 5. 最后的取舍总结

页面结尾回到决策，明确列出：

- **拿走什么**：准备进入本项目的模块或模式；
- **重写什么**：能力有价值但边界不合适；
- **不要拿什么**：许可证、安全性、耦合或质量不达标；
- **下一步**：需要运行的最小 PoC 或需要导出的功能包。

## 核心交互：选功能后导出 Skill 与代码索引

这不是“下载一份 Markdown”，而是把研究结果转换成 Agent 的工作上下文。

### 用户流程

1. 在项目 HTML 中勾选一个或多个功能。
2. 选择目的：学习、对比选型、移植实现、重写实现或补测试。
3. 系统把所选功能的证据合并、去重并固定到分析时的提交。
4. 导出人可读索引与 Agent Skill。
5. Agent 开始工作前先验证提交和文件是否仍匹配；不匹配则标记索引过期并重新生成。

### 导出包必须包含

| 产物 | 作用 | 核心内容 |
|---|---|---|
| `SKILL.md` | 告诉 Agent 何时使用、如何完成任务 | 触发条件、目标/非目标、阅读顺序、工作步骤、验证门、完成标准、风险限制 |
| `module-index.md` | 给人快速审阅 | 功能与模块关系、为什么读、复用建议、阅读路线 |
| `module-index.yaml` | 给 Agent/工具稳定解析 | 仓库/提交/许可证、功能、模块、文件、符号、调用关系、测试、依赖、证据强度 |
| `feature-tour.tour`（可选） | 在编辑器中逐步播放 | 固定提交下的文件、行/模式锚点与讲解顺序 |
| `evidence.md` | 保留研究边界 | 已验证事实、未验证项、冲突证据、生成时间 |

### 模块索引的最小字段

索引至少要能表达：

- 仓库身份、固定 commit、许可证和分析时间；
- `feature_id`、功能目标和用户可见结果；
- 入口点、核心模块、关键文件和关键符号；
- 控制流或数据流的先后关系；
- 对应测试、运行命令和成功证据；
- 外部服务、模型、协议和配置依赖；
- 复用等级、需要改造的边界和禁止直接复制的部分；
- 建议阅读顺序，以及每一步“读完要回答的问题”。

单纯的文件列表不合格。GitHub 的代码导航把定义、引用和符号作为核心导航单位，因此索引也必须落到符号和引用关系，而不只落到目录。

### 导出 Skill 的工作方式

导出的 Skill 应是“任务 Skill”，不是仓库百科：

1. **触发**：当 Agent 要实现或比较已选功能时使用。
2. **定界**：写清哪些能力属于本包、哪些不属于，避免通读整个仓库。
3. **阅读**：按黄金路径读取入口、核心实现、状态和测试；每一步带问题。
4. **执行**：先复现当前行为，再移植或重写；不得把参考仓库直接当安全边界。
5. **验证**：检查固定提交、运行最小测试、核对许可证与未验证假设。
6. **结束**：产出实现决定、变更证据、剩余风险；发现漂移时停止引用过期索引。

## 可参考的现成方案，以及只借哪一部分

| 参考 | 最值得借的部分 | 不直接照搬的原因 |
|---|---|---|
| GitHub `acquire-codebase-knowledge` Skill | 先读项目意图、七类事实文档、每项结论必须有证据、未知项显式标记 | 输出偏全面审计，仍需要更强的产品能力优先级和复用取舍 |
| GitHub `code-tour` Skill + Microsoft CodeTour | 面向角色的有序源码导览；真实文件/行/模式锚点；可校验和固定 `ref` | 适合“怎么读”，不负责判断“值不值得复用” |
| DeepWiki | 允许通过 `repo_notes` 和指定 `pages` 控制重点，避免大仓库自动聚类遗漏关键模块 | 默认 Wiki 仍可能按知识覆盖组织，不一定按用户任务组织 |
| CodeWiki | 大仓库分层拆解、`--focus` 聚焦模块、增量更新和 HTML 输出 | 可作为生成底座，不能替代首屏决策和功能复用卡 |
| RepoAgent | 文档和代码变化联动、增量维护 | 以仓库文档为中心，不等于面向选型和移植的教学页 |
| GitHub Code Navigation / Sourcegraph Cody | 符号、定义、引用和代码图；按当前任务选择相关上下文 | 它们是索引/检索底座，不是最终讲解结构 |

## Skill 选择结论

### 现在可直接借鉴

1. `github/awesome-copilot@acquire-codebase-knowledge`：作为“事实采集与证据门”。
2. `github/awesome-copilot@code-tour`：作为“所选功能的阅读路线导出器”。
3. `github/awesome-copilot@architecture-blueprint-generator`：只在进入深度架构模式时补充，不用于默认首屏。

### 我们仍需自建的 Skill

暂名 `repository-teacher`，负责现有 Skill 没覆盖的三个判断：

- 从源码事实提炼真正的产品能力，而不是复制 README 标题；
- 用总—分—总结构生成面向决策的 HTML；
- 把用户选中的功能打包成 `SKILL.md + module-index + tour + evidence`。

当前先定义它的输出契约；等你确认本产品范围后再实现 Skill，不在研究阶段提前做完整技术模块设计。

## 第一版做到什么程度

### MVP 必须有

- 导入一个本地完整仓库，并固定到 commit；
- 识别并人工可修正 5–10 个真正的产品能力；
- 生成一份总—分—总的项目 HTML；
- 至少讲清一条黄金路径；
- 每个功能卡都有关键符号、测试与证据链接；
- 用户可选择功能并导出 Skill 与人/机两份模块索引；
- 代码发生变化后能判断索引是否过期。

### MVP 明确不做

- 不做通用 IDE；
- 不做自动执行或修改参考仓库；
- 不做 ACP/Codex/OpenCode 派单；
- 不追求一次生成整个仓库的所有知识；
- 不在这一阶段设计完整知识图谱、数据库或微服务拆分。

## 验收标准

用一个陌生仓库做冷读测试：

1. 读者在 30 秒内能说出项目是什么、最值得借什么、最大风险是什么。
2. 读者在 3 分钟内能复述一个核心功能从入口到结果的实现主线。
3. 页面中每个关键实现判断都能跳到文件、符号或测试证据。
4. 读者选择一个功能后，系统能导出完整的 Skill 和模块索引。
5. 一个未参与研究的 Agent 只读取导出包，就能找到正确的实现入口与验证命令。
6. 当仓库 commit 或关键文件改变时，导出包不能悄悄继续声称有效。

## 一手资料

- [GitHub Awesome Copilot：Agent Skills 与代码库相关 Skills](https://github.com/github/awesome-copilot/blob/main/docs/README.skills.md)
- [Acquire Codebase Knowledge Skill](https://github.com/github/awesome-copilot/blob/main/skills/acquire-codebase-knowledge/SKILL.md)
- [Code Tour Skill](https://github.com/github/awesome-copilot/blob/main/skills/code-tour/SKILL.md)
- [Microsoft CodeTour](https://github.com/microsoft/codetour)
- [DeepWiki steering](https://docs.devin.ai/work-with-devin/deepwiki)
- [GitHub：使用 Copilot 探索代码库](https://docs.github.com/en/enterprise-cloud@latest/copilot/tutorials/explore-a-codebase)
- [GitHub Code Navigation](https://docs.github.com/en/repositories/working-with-files/using-files/navigating-code-on-github)
- [Sourcegraph Cody Context](https://sourcegraph.com/docs/cody/core-concepts/context)
- [RepoAgent](https://github.com/OpenBMB/RepoAgent)
- [CodeWiki](https://github.com/FSoft-AI4Code/CodeWiki)

