# RepoLens 项目规则

本仓启用 Ponytail full：先完整理解真实执行链，再依次检查“不需要新增 → 复用仓内实现 → 标准库/原生能力 → 已有依赖 → 最小正确改动”。修缺陷必须修共享根因，不按单个症状堆补丁。不得简化信任边界校验、数据安全、错误恢复、无障碍或用户明确要求；每个非平凡改动保留最小可运行回归测试。

## 通用 Pipeline 修复原则

RepoLens 是面向任意代码仓库的通用索引 Pipeline。禁止根据仓库名、README 中的领域名、截图文字、路径关键词、正则或某个样例项目写专用分支、过滤表、排序表或 Prompt 条款。Coze、Waku、Pipecat 等仓库只能作为黑盒验收样本，不能进入产品规则。

遇到功能识别或报告内容错误时，必须先定位到通用阶段：代码图事实、证据包、项目定位、业务能力归并、语义审校、章节生成或人类可读性审校。修复应改变该阶段的通用输入/输出合同或不变量，使所有仓库沿同一条 Pipeline 获益。

顶级业务功能必须同时具备：独立使用者目标、独立可见结果、独立业务状态或责任边界、源码证明的完整因果链。页面、路由、CRUD、登录、健康检查、Bridge、类、目录和示例不是天然业务功能；不能独立交付产品结果时，只能成为某项业务功能的实现证据、支撑模块或排除项。

每个通用修复至少保留两类回归证据：原失败仓库的真实/缓存重放，以及一个不同产品类型的最小合成反例。测试必须断言业务语义和因果边界，不能只匹配标题关键词。
