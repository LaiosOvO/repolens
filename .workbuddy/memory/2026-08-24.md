
## 参考仓库补齐 + md 效果保证条款落地（87c720e）

- 新 clone/拉取：`参考实现/pocketflow-tck/`（nodes.py 880 行全管线 + docs/Codex 样例 2 篇）与 `参考实现/opendeepwiki/`（AIDotNet/OpenDeepWiki 的 4 个 prompt 模板 2528 行 + Agent/CatalogTool/GitTool/DocTool/FilePromptPlugin）。codeload/git clone 仍 137 被杀，全走 jsDelivr 逐文件（curl 循环连发会被杀，须逐条命令单发）。
- OpenDeepWiki 准确仓库名是 AIDotNet/OpenDeepWiki（jsDelivr 搜 OpenDeepWiki/OpenDeepWiki 404）。
- 精读结论写入 docs/reference-implementations-notes.md §7-§9（五家对比表 + 落地条款）。
- SKILL.md 阶段三新增「md 效果质量条款」小节：读者心智模型 / 中心用例驱动 / 代码块 <10 行 / 章节衔接互链 / mermaid 语法细则+图型选型 / 薄页禁令 / 标识符不翻译；完成门加自检。设计依据段同步提及两家新参考。
- push 仍被 HTTP2 framing layer 网络错误挡住，本地 4 个 commit 待推。

## lathe 参考吸收 + GLM glm-5.3 引擎接入（450baff）

- 新参考仓库 lathe（devenjarvis/lathe，808★，skill+CLI 同构）已拉到 `参考实现/lathe/`（主 SKILL.md 415 行 + lathe-verify 49 行，jsDelivr 逐文件）。
- lathe 四条教学法落进 repo-learning SKILL.md 阶段三：先错后正、渐隐式讲读、Ground-or-flag 的 load-bearing 限定（细化行号接地铁律：只对 load-bearing 事实强制，禁满页 hedge）、标题命名产物+禁开场白；完成门自检同步扩充。
- GLM coding plan 接入调研完成（笔记 §11）：三端点（OpenAI 兼容 `api.z.ai/api/coding/paas/v4` / Anthropic 兼容 `/api/anthropic` / Codex `/api/v1`），model=glm-5.3，`[1m]` 后缀开 1M 上下文，thinking 不可关闭只调 reasoning_effort，非高峰（工作日 14:00-18:00 UTC+8 之外）积分 5 折。
- 新建 `tools/glm_call.py`：OpenAI 兼容批量调用器，prompt 哈希缓存（原子写+断点续跑）、指数退避、逐日日志；--check-env 已验证通过。**机器上无 ZAI_API_KEY**（zshrc/zshenv/history/Codex config/Claude settings/keychain/aivr key 全查过，aivr key 对 z.ai 401）——待用户提供。
- SKILL.md 新增「GLM 批量生成引擎条款」：GLM 只做蒸馏/汇总/格式化机械层，产物须行号反查抽检；证据/账本责任不转移；不可用时降级 agent 执行不阻塞。
- push 本轮成功：09b5d98..450baff 已推到 origin/skill/repo-tutorial-script，此前积压的 3 个 commit 全部上远端。

## repolens 产品自测：CLI 装配 + 索引实跑 + 增量复用 + UI（2026-08-24 凌晨）

- `repo-teacher` 已 editable 安装进 workbuddy venv（envs/default），`--no-deps` 一次成功；12 个子命令齐全（index/report/inventory/ui/compare/explain/graph/export-skill/validate/prepare-report/render-report/serve）。
- 小仓自测：pocketflow-tck（10 文件/40 符号/557 关系/3 模块）一次通过。
- 真实大仓自测：eigent（1266 文件/4905 符号/33556 关系/13 模块）首跑成功；第二遍**增量复用验证通过：reused 1266 / reanalyzed 0**。产物在 /tmp/repolens-selftest/eigent/（index.html 18.6MB、index.json 20.8MB、capability-graph.json 11.9MB，generation 目录 + current 符号链接的原子发布结构）。
- **坑（复测）**：前台跑第二遍索引连续 3 次被 137 杀且 stdout 全空——与既有「T7 外接盘 sqlite 慢 + 前台长任务被杀」同族；**后台 run_in_background + 日志文件跑法一次成功**。此坑已二次确认，长索引任务一律后台跑。
- UI 服务器 `repo-teacher ui --port 8787` 启动正常（HTTP 200，中文界面「Repo Teacher · 本地报告生成器」），后台任务 0vCqYn 仍在跑。
- 遗留观察：eigent 1266 文件但只有 13 模块——模块归并粒度值得对照 .codegraph 的 152 目录口径复核（不影响本次验收）。

## loopx 仓 repo-teacher 索引（2026-08-24 凌晨）

- 位置：`/Volumes/T7/kb/repo/agent-cli与运行时/loopx`；产物 `/tmp/repolens-selftest/loopx/`（原子发布结构，current 符号链接）。
- 规模：2830 文件 / 20231 符号 / 232953 关系 / 13 模块；index.html 74MB、index.json 97MB、capability-graph.json 71MB（六仓实测中最大）。耗时约 10 分钟（后台跑法）。
- **新坑（重要）**：首次索引在 3m55s 收尾时被稳定性校验拒绝——`repository changed while it was being indexed; retry from a stable working tree`。判定链是三段 manifest（含每文件 mtime_ns/ctime_ns）+ 双 scan + 三 snapshot 全等（indexer.py:747 `_source_is_stable`），T7 外接盘上长索引期间任何元数据抖动都会触发 fail-closed 保护。**处置：确认无真实变更（git status 干净、双 manifest 相等、无 .DS_Store）后删 lock 重试即成功**。非 bug，是保护误伤；若频繁复现可考虑 manifest 排除 ctime 或对非 Git 目录放宽。
- 观察留档：13 模块口径中 deprecate(481)/examples(587)/docs(362) 占了近半文件——与 eigent 的「模块归并粒度」观察同族，模块归并把 docs/examples 也算模块，教学视角下这类目录应归入「非源码资产」，值得进产品 backlog。

## loopx 索引报告 md 版产出（2026-08-24 凌晨）

- 用户要求「只要 md」：从 `/tmp/repolens-selftest/loopx/current/index.json` 蒸馏出 `/tmp/repolens-selftest/loopx/index-report.md`（165 行 / 8 节），未走 narrative 模型管线（report 子命令需 Codex/OpenCode/DeepSeek 之一做章节合成，且默认出 HTML）。
- 报告结构：项目定位（README 原文引证）→ 规模表 → 13 模块地图（源码/非源码资产分层）→ loopx/ 下钻（control_plane 265 文件 12 子域、capabilities 258 文件 19+ 子域、6 harness goal_mode 适配）→ 全量 12 入口账本（主 CLI/能力域 14 CLI/扩展包 3 CLI/脚本）→ 12 站阅读路径（含手工修正建议：deprecate 站跳过、control_plane 补位）→ mermaid 架构图 → 5 个选题候选 → 证据边界。
- md 效果条款应用：中心用例（quota-replan 烟雾脚本）、代码标识符不翻译、薄页禁令（每节都有实质内容）、Ground-or-flag（heuristic 边占比 82% 如实声明）。
- 观察：索引自动 features 是浅层入口账本（机械层），深读教学内容在 loopx-teaching/report/report.md（884 行）——两者互补关系已在报告 §8 交叉引用。

## 产物落位修正（用户指出 /tmp 不当）

- 用户质疑 loopx 索引产物为何放 /tmp：那是上轮产品自测的临时目录，正式交付不该沿用（/tmp 重启即清）。已整体搬迁到 `loopx-teaching/report/repo-teacher-index/`（242MB：index.html 77MB + index.json 97MB + capability-graph.json 74MB + index-report.md，原子发布结构与符号链接完整保留）。
- 教训入册：**repo-teacher 产物一律落 `{仓库名}-teaching/report/`，自测才用 /tmp**。六仓正式索引后续都按此口径。

## index-report.md 补全业务功能分析（用户指出内容缺失）

- 用户指出第一版 index-report.md「只有目录分析、业务功能去哪了」——根因：只蒸馏了索引机械层（modules/features/reading_path），没并入 stages 深读账本里的业务功能内容。索引的自动 features 本来就只是浅层入口账本（12 个 entrypoint/cli-command），业务功能语义在深读材料里。
- 重写为完整版（283 行 / 11 节）：并入 loopx-teaching/report/report.md（884 行深读报告）与 stages/02-product-surfaces.md（106 origins → 27 surfaces）的 §5 业务功能 10 条全量 + §6 核心功能 6 个逐个拆解（每个含一句话本质 + 关键机制 + 边界）+ §7 tick 完整链路 + §11 覆盖账本。
- 结构：项目定位 → 规模 → 13 模块地图（loopx/ 下钻）→ 入口账本（含 60 CLI 命令组全量）→ **业务功能 10 条** → **核心功能 6 个** → tick 链路 → mermaid 架构图 → 阅读路径 14 站（手工修正版）→ 选题 6 个 → 覆盖账本。
- 教训：**索引报告 ≠ 教学报告**。repo-teacher index 的 features 是机械层账本，合成完整 md 时必须并入 stages 深读材料的业务语义层，否则就是「有骨架没血肉」。

## md 代码引用链接化工具（093d5fa）

- 用户要求报告 md 里的代码引用可点击跳转 → 做成通用工具 `tools/md_code_links.py`（不针对单份报告）：把 `文件:行号` 引用转成相对路径 markdown 链接 + `#L行号` 锚点，VS Code/GitHub/GitLab/Typora 点击即定位。
- 设计要点：两轮扫描（反引号引用可无行号 / 裸引用必须带行号防误伤）；解析链 带行号alias > 裸alias > 仓根 > md目录 > basename 全仓唯一；basename 搜索默认排除 deprecate/（loopx 481 个废弃文件是歧义主源）；行号越界 fail-closed；防重入。
- loopx index-report.md 实跑：47/47 全部链接化（12 条 alias 消歧：cli.py→loopx/cli.py、quota.py→loopx/quota.py、runtime.py→extensions/runtime.py 等，presets.py 两个候选取根模块门面 loopx/presets.py）；全量验证 47 个链接目标存在于磁盘、mermaid/结构树零误伤、行数不变。
- 通用性：其他五仓报告同法可做（alias 表按仓写一次）。工具已 commit 待 push。

## repo-learning 新增阶段零：上游热更新合同（635e890）

- 用户要求：生成教程前先 git pull + skill 加热更新功能 + 每次告知更新内容。已固化为 repo-learning SKILL.md「阶段零：上游热更新」。
- 合同要点：fetch→比较→ff-only pull（本地脏禁 pull）；`stages/00-upstream-update.md` 变更报告（commit 列表/目录分组统计/产物影响映射）+ 回复中直接向用户汇报；影响四级 High/Medium/Low/Skip（吸收 OpenDeepWiki incremental-updater 915 行的分类表）；最小影响原则不全量重写；禁编造变更；pull 后先 codegraph sync；网络失败降级不阻塞；完成门加热更新自检。
- 顺带精读了 OpenDeepWiki incremental-updater.md 全文（参考实现/opendeepwiki/prompts/），此前只拉了文件没读。

## 官方文档语义层补齐（loopx 教程修订）

- 用户批评：loopx 教程完全没参考飞书官方《LoopX 用户手册》。属实——只蒸馏了代码索引+stages 深读，把 docs/ 当非源码资产跳过
- 飞书 wiki 是 JS 虚拟化渲染：普通 WebFetch 只拿得到标题；ego-browser 滚动逐屏收集（500px 步进，按 data-block-id 去重累积）拿全 551 block/6.3 万字，脚本存 /tmp/feishu_collect.js（模式可复用）
- 差距关键词（修订前 grep 命中数）：Explore Graph/Harness 0、Capability Pack 0、Domain State 0、HITL 0、数字员工 1
- 修复：index-report.md 357 行（283→357），新增 §6.7 三层架构、§6.8 长程能力增强，§1/§5/§9/§10/§11 全部强化；13→业务功能、6→8 核心功能；每个官方概念找到源码对应物（domain_state.py、goal_vision.py 预算、replan_rules.py 17 规则、explore 三支柱、openviking provider），14 处新引用行号全部验证在界内
- SKILL.md 固化「官方文档语义层条款」（commit b31f430）：docs/ 是第一语义来源；术语对齐义务；飞书类 JS 页用浏览器抓

## 通俗版功能全解 + 链接化工具修复（凌晨批次）

- 用户要求：结合官方文档（飞书手册）和仓库 docs/ 文档，把 loopx 功能列举完整、讲通俗。产出 `loopx-teaching/report/features-guide.md`（约 300 行通俗版功能全解）
- 三方底数：飞书手册 + docs/ 325 篇（49 子目录）+ CLI 解析器实测 111 个顶层子命令（build_parser 注册 51 组）+ `loopx capability list` 17 内置能力（active-preview 13/experimental 3/compatibility-facade 1）+ 官方命令分组 `loopx commands`（五组：Start here/Daily operator/Loop driver hints/Setup and automation/Maintainer and adapter）
- 功能全解结构：§0 公司类比表（人事经理/考勤/审批门禁）→ §1 痛点三伤 → §2 六层架构 → §3 完整功能清单（17 能力分六组：治理/工程/记忆决策/扩展连接/宿主接入六工具/运维面板）→ §4 核心概念人话对照（Goal/Turn/Gate/Quota/Attention Queue/三层/Loop Agent 第一屏五问）→ §5 心跳旅程 → §5b 管理面三设计（Agent Work Feed/非技术运营者状态模型/双面采纳）→ §5c 四大能力场景 → §5d 公私边界 → §6 代码地图
- **md_code_links.py 首版 bug 修复**（commit 526be4f）：轮1（反引号引用）的匹配起点永远落在 codespan 掩码区间被 blocked() 拦死——之前 75 条链接全是轮2（裸引用）功劳，反引号引用从未成功。修复：blocked() 加 codes 开关，轮1 跳过 codespan 检查（linked_spans 仍拦已链接形态）。四形态测试全过
- 重跑链接化：features-guide 37 条；index-report 75→102 条。残留未链接均为刻意（.loopx/registry.json 运行时文件、历史指路、自引用）
- 发现并修正 index-report §4.2 表错误：packages/ 三个包的 cli.py 实际在 src/<pkg>/cli.py（各 212/145/113 行）

## loopx 热更新第 2 轮执行（上午批次）

- 用户问「不是有重新索引吗」→ fetch 发现上游 +124 commits（4b76262→f66182fd，v0.5.1→v0.5.2，573 文件 +83860/-5617），执行完整热更新链
- 教学影响面 Top10 已写入 loopx-teaching/stages/00-context.md（TS 化 control_plane、authority 集中化、quota 收据回放、goal-channel 默认通知、periodic-report milestone、benchmark four-arm、dsh 前端插件、attached_session、SonarCloud、benchmark skill）
- **顺序执行铁律再犯**：repo-teacher 与 codegraph 并发跑导致稳定性校验 fail-closed（codegraph 写 .codegraph/ 触发 repo-teacher 文件变化检测）。第二次又抢跑 codegraph 被手动停掉。正确顺序：pull → repo-teacher index → codegraph sync，每步等上一步完成
- codegraph sync 首次爆栈「Maximum call stack size exceeded」，但重跑成功（可能为瞬时深层解析问题）；验证法：query 新增文件 attached_session 确认入库
- 行号漂移修正 4 处（cli.py:880→328、cli.py:314-392→218/348-412、should_run.py:157-272→119-349/226-349）+ §2/§3.2 规模数字更新；todos.py:181-199/246-262 与 quota.py:614-779 验证后无漂移
- 新索引：3039 文件/21761 符号/244641 关系，generation 851baaf；全量链接验证 102 条/0 缺失/0 越界

## 单仓单夹合同固化 + 六仓产出目录归位（用户指出产出散落）

- 根因：四个 skill 的 OUTPUT 缺省各自为政（repository-report→{repo}-system-explainer/、repo-learning→{repo}-learning/、repo-script→{repo}-script/），一个仓库产出散成多个兄弟目录
- 合同固化（commit 6b536c1 已推送）：统一为 `{repo}-teaching/` 单根目录——repository-report→report/、repo-learning→learning/、repo-script→script/、repo-video→video/；各 skill 加「发现散落布局先迁移归位再开工」条款
- 现场归位（备份在 /Volumes/T7/kb/repo_备份_2026-08-24_单仓单夹迁移/，11 目录 diff 校验通过）：4 个空壳 *-tutorial-script 删除；Proma 两目录→Proma-teaching/{report,script}；codex-learning→codex-teaching/learning；4 个 system-explainer→各自 teaching/_legacy/system-explainer
- 修引用：Proma 两个 browser-check + codex-learning 三个 stages 的旧绝对路径、Proma report HTML 品牌标签；产出进度.md 三处路径更新；六仓 grep 零残留

## 本地目录整理（用户意图澄清：只管 skill 教程产物归位，素材库不动）

- 用户三个答复：①「只是想让 skill 生成的教程全部放到一个目录里」——不是要整理素材库；②面试目录先要详细清单；③备份先留、根目录 HTML 收走
- 归位：`视频生成/hyperframes-learning.md` → `视频生成/hyperframes-teaching/learning/learning.md`（repo-learning 产物散件，最后一处）
- HTML 散件 `AI_native转型案例总结.html` → `代码教程整理/`
- 终检：全 repo 范围 learning/report/manuscript/features-guide 四类产物在 *-teaching/ 外散件 0 处
- 未动：13 个 UP 素材目录、面试 43G（清单已出：顶层 14 个日期命名录屏 mov/wav + raw/ 选择/ 等待转录/ 子夹）、迁移备份 10M（用户要求先留）
- up-439507793 身份仍未识别（纯 mp4 无元数据），待用户告知 UP 名后可改名

## loopx report.md 合并 + 第 0 章用户上手（用户要求）

- 用户指令：features-guide 与 report 合并；补「身为用户怎么使用」为第一章；web 界面用 ego-browser 截图
- 实跑取证（PyPI loopx 0.5.0 装进 workbuddy venv，playground=/tmp/loopx-playground/my-app）：doctor→connect→todo add→status→quota should-run→todo claim→refresh-state→spend-slot 全链
- 实跑踩坑（教学素材）：①user todo 必须带 --task-class user_gate/user_action；②claim 前必须 configure-goal --registered-agent；③spend-slot 是 receipt-bound 链（should-run 收据→同 turn_instance_id+todo_id→refresh-state 带 --delivery-outcome 四枚举值→todo 声明 task_repository 写合同否则 workspace 因果守卫拦截「todo_write_contract_not_explicit」）；④结算还需 contract health ok（gitignore .loopx 与 .codex 后 check warnings=0）
- 新产物：report.md 插入「第 0 章 · 身为用户怎么使用 LoopX」（10 分钟上手：安装体检/connect/日常看批/dash 面板/循环命令链/六宿主表/命令速查）+「速览篇」（features-guide 通俗内容全部并入，含公司类比/六层架构/17 能力人话/术语对照/心跳旅程/公私边界）；features-guide.md 改为 9 行薄索引指向 report.md；report.md 884→1070 行
- 新截图：screenshots/run-dash-first-screen.png（ego-browser 实拍 Session Dashboard，DOM 验证 1 GOALS/1 NEEDS YOU/3 OPEN TODOS/live·read-only 徽章）；dash 服务须 run_in_background 保活否则沙箱回收（nohup 会被杀，两次 chrome-error 截图教训）
- 全量链接校验：report.md 相对链接（../screenshots 与 ../../loopx 源码）零 MISS，内链锚点「#功能三配额决策与幂等结算」有效

## loopx report.md 0.8 节：三角色演示 + codex 模型/effort（用户要求）

- 用户指令：①用测试数据把功能场景演示一遍；②对照说清 codex AGENTS.md 的调度/实现/验收流程；③codex 指定模型和 effort 的方法写进去
- 关键澄清：codex 的 AGENTS.md 是「给 AI 编码助手的仓库贡献规则」（Rust 风格/测试规范），不是多 agent 分工协议；codex 本身是单 agent CLI，LoopX 才是给编码 agent 补调度/验收控制面的
- 三角色演示全链实跑通（playground + 独立 worktree）：configure-goal 注册两 agent（逗号分隔，逐个注册会覆盖！）→ todo_lifecycle_authority 委派验收权给调度者 → user_gate 验收门绑死实现者 → should-run 被门拦（gated_delivery）→ 用户在 ACTIVE_GOAL_STATE.md 勾选放行（调度者代开被 bound_agent_mismatch 拒，委派权也碰不了 user_gate）→ 实现者独立 worktree 干活+commit → refresh-state 收据链闭合 → 调度者 todo complete --authority-reason 验收关单 → spend-slot --result-hash=commit-hash 结算
- 踩坑实录（已全部写进文档当教学素材）：--registered-agent 覆盖语义；refresh-state 需同 turn should-run 心跳收据；workspace 守卫要求独立 git worktree（--delivery-workspace-path 或从 worktree 内跑）；worktree 需要 git 基线提交（--allow-empty init）
- codex 模型/effort 实测：-m 指定模型；-c model_reasoning_effort= 指定力度（枚举 none/minimal/low/medium默认/high/xhigh/max/ultra，源码 codex-rs/protocol/src/openai_models.rs:50）；TUI 内 /model 选择器；~/.codex/config.toml 配置层（本机 gpt-5.6-sol + xhigh）
- loopx codex-cli-exec-handoff 实测：headless exec 移交默认禁用（防 hidden execution），走 codex-cli-bootstrap-message --message-only 生成 TUI 粘贴文本
- report.md 1070→1253 行；全部相对链接零 MISS

## skill 第 0 章合同 + loopx 第 3 轮热更新重索引（用户要求「修改 skill 然后重新索引」）

- repository-report SKILL.md 两块新合同（commit 0f0d7ce，未 push）：①**第 0 章用户上手（必做）**——report.md 第一章前必须有「身为用户怎么使用 {项目}」，四件套=安装体检/最小上手链/日常操作面/命令速查，有 web UI 必须 ego-browser 实拍（服务保活必须 Bash run_in_background，nohup 会被沙箱回收拿 chrome-error），SDK 类项目改为最小可运行示例；②**功能演示实跑取证（必做）**——写功能章前建 playground 真跑：环境隔离进 workbuddy venv、/tmp playground、全链真实输出、多角色用测试数据走通（git 类用独立 worktree）、报错即素材但须给正确用法、禁止编造输出。完成门同步加两条检查项，固定顺序第 8 步组装序列头部加「第 0 章用户上手」
- loopx 上游第 3 次更新：f66182fd→dad3d0a6（4 commits：fix(quota) settle repaired todo delivery contracts、fix(cli) accept doctor subcommand output format、CLI 参数诊断、quota slot accounting 测试 56 行）；ff-only pull 干净完成
- repo-teacher 索引重跑：后台 run_in_background 跑法（任务 0w6QP6），输出仍落 loopx-teaching/report/repo-teacher-index/（覆盖旧 generation 851baaf，原子发布结构）
- 执行结果：索引 3m14s 成功，增量复用 3033 文件/重析 6 文件，新 generation `37a68022`（3039 文件/21764 符号/244661 关系）；codegraph sync 5.6s（199 节点，新测试 test_quota_slot_accounting.py 已入库验证）
- 行号漂移修正 7 处（上轮遗留漏网）：report.md 4 处 + 498 行 1 处 + index-report.md 2 处——`cli.py:314→328 main`、白名单 `347-384→363-396`、全局回退 `388-392→400-403`、解析链 `334-392→334-403`、`348-412→334-407`；全量链接校验 report.md 127/127、index-report.md 102/102 零真实缺失（4 条目录链接被 `-f` 误报，目录实际存在）

