# 2026-08-23

## 业务上下文梳理（新会话冷启动）

用户把四类材料的绝对路径交给我，要求通读后确认"业务文档需要输出什么"。已通读：

- 源头材料 4 份：全栈观察员-教程内容.md（786 行，Claude Code/superpowers/gstack/Open Design/AI 接口五大战役）、Leo与Frank-Agent工程教程.md（Harness/Loop/Graph 体系）、小哲-Agent面试题库129题.md（93 题去重）、douyin_全栈观察员/转录合集.md（99 视频索引）
- 融合 skill：repolens/skills/repo-teaching/SKILL.md（一次取证 → 报告+稿件双产物 → ego-browser 验收）
- 总控手册：/Volumes/T7/kb/repo/产出进度.md（6 仓首轮完成 ✅，codex 报告补齐中，真实运行截图仅 loopx 落地）
- repolens/PRODUCT-BRIEF.md：产品愿景是"仓库教学与复用索引"（报告 HTML → 选中功能 → 导出 Skill+索引给 Agent）

## 业务判断

用户在做"代码仓库教学"内容生意：用 repo-teaching 管线对 AI/Agent 开源项目做源码级拆解，产出讲解报告（深度图文）+ 视频教程稿件（口播），风格对标抖音博主全栈观察员（1.4 万粉，同赛道）。6 仓产出是存量内容资产；缺口在"从能录到能发"的运营层文档与选题规划。

## 身份引导

BOOTSTRAP.md 仍在，已向用户发起身份确认（称呼/名字/风格/城市），待回复。IDENTITY.md、USER.md、SOUL.md 均为空模板，~/.workbuddy/MEMORY.md 不存在。

## 六集发布物料 + skill 扩展（任务 3、4 完成）

用户选定「③ 六集发布物料包」，即 skill 要实现的第二个功能：报告/稿件之外再产出投稿物料。

- 新建合同 `skills/repo-teaching/references/publish-kit.md`：6 固定块（标题三版 ≤30 字 / 封面文案 主标≤12 副≤16 / 简介五段 / 三层标签 6–9 个 / 分段时间轴 / 置顶评论）+ 质量门（数字与稿件三场景一致、三方数字不打架）。
- 六份 publish.md 全部产出：codex/eigent/loopx/prime-agent/cumora 在各自 `script/publish.md`，Proma 为旧布局在 `桌面agent应用/Proma-tutorial-script/publish.md`。置顶评论的仓库地址与 commit 均对照各仓 `stages/00-context.md` 核验（codex@536f86e、eigent@d308955、loopx@4b76262、prime-agent@c75a637、cumora@2c9d17e、Proma@7f5c100）。cumora「7 agent 零碰撞、28s」已溯源到证据文件（COORDINATION.md:738-746）。时间轴 = 各节字数 ÷ 400–430 字/分钟，按官方时长等比缩放。
- SKILL.md 扩展：frontmatter 改为四成品；阶段三新增第 15 步「发布物料」；阶段四重编号 16；完成门加 publish-kit 质量门与 `script/publish.md`（并发有另一改动加了第 14 步分镜交接稿 storyboard.md，已兼容合并）。
- `/Volumes/T7/kb/repo/产出进度.md` 已同步：总目标改四份成品、六仓状态表登记发布物料、分支提交状态。
- git：本地提交 8f47e37（含 storyboard.md 一起入库）；push 遇代理 502 失败，已在进度手册记待办。

坑：SKILL.md 编辑期间被外部并发修改两次（另有 agent 在加 storyboard 步骤），Edit 前必须重读；本机 Python 正确路径是 `/Users/admin/.workbuddy/binaries/python/versions/3.13.12/bin/python3`（envs/default 不存在）；zsh 里 `echo ===` 会解析报错，用 `---` 分隔。

## codex-learning 用 repo-learning skill 出学习报告（任务 5、6 完成）

用户要求用 `skills/repo-learning/SKILL.md` 对 openai/codex 出学习报告。本地克隆 `agent-cli与运行时/codex` @536f86e（2026-08-21 快照），codegraph 已建索引。复用 `codex-teaching/stages/` 完整证据基座（22 surfaces / 9 CF / 10 BF / 44 evidence / 9 fingerprint），按 repo-learning 阶段一产出 11 个 stages 文件。

**实跑亮点**（阶段二）：
- 全局没装 codex CLI，`npm install -g @openai/codex` 安装成功（v0.149.0，50s）
- `codex login status` → "Logged in using ChatGPT"
- `codex --help` → 23 个 subcommand 完整列表
- `codex exec --sandbox read-only --skip-git-repo-check` → banner + config dump 完整输出（model=gpt-5.6-luna、sandbox=read-only、session_id 生成）
- `~/.codex/sessions/2026/08/23/` 出现 4 个新 rollout JSONL（每次 exec 唯一 session_id、sessions/yyyy/mm/dd/ 分桶）
- `codex sandbox echo "hello"` → `sandbox-exec: sandbox_apply: Operation not permitted`（证 Seatbelt 路径在工作；环境本身被外层沙箱包）
- **跑不通**：模型 API 调用被代理阻断（chatgpt.com/backend-api socket hang up），`codex doctor` 同样挂起——环境问题不是 codex bug

**截图**（6 张，Pillow + SFNSMono 渲染）：
- `run-version-login.png`、`run-help-subcommands.png`、`run-exec-startup.png`、`run-sandbox-seatbelt.png`、`run-sessions-rollout.png`、`run-codex-home.png`

**learning.md**（614 行 / 44KB）8 节结构：项目是什么 / 怎么跑 / 功能全貌 / 9 核心功能逐个讲（每节有痛点→用法→逐跳因果→坑）/ 架构与工程地图 / 7 阶段学习路径 / 5 个候选选题 / 证据边界。所有 `文件:行号` 证据都对照 evidence.md 与 mechanism-fingerprints.md；§8 诚实列出未完成项（模型回包、TUI 真截、F8 长任务触发）。

**新增约定/坑**：
- codex 装包首选 npm（`@openai/codex`），源码构建 cargo build 会花数十分钟且需额外拉依赖——除非你要测特定 commit 版本
- macOS 终端截"实跑图"用 Pillow 渲染即可（不用 ego-browser，CLI 没有 Web UI）
- 终端里"模型启动后挂死"绝大多数是网络代理问题，先看 `~/.codex/log/` 和 `chatgpt.com` 的连通性
- `--skip-git-repo-check` 是在 /tmp 跑 exec 的必备 flag，否则 "Not inside a trusted directory"

## codex-learning 用户反馈 → 工程目录结构重盘（21:10 后）

用户看完 learning.md 的「业务功能 10」表提出两点反馈：
1. BF 太少，才 10 个
2. B10「IDE/远程集成」= `codex app-server`，不是单功能，是个集成层

我重新盘点 `codex-rs/` 的 **102 个 crate**，按能力域分组归并，发现真正顶级业务功能应为 **17 个**：

- **B10 是集成域而非单功能**：下面挂 9 个 crate（app-server / app-server-protocol / app-server-protocol-noop-macros / app-server-transport / app-server-client / app-server-daemon / app-server-test-client / stdio-to-uds / uds），把 core 能力以 JSON-RPC 暴露给 IDE/桌面/远程
- **遗漏的 7 个顶级 BF**：
  - **BF11 Code Mode**：4 个 crate（code-mode / code-mode-host / code-mode-protocol / code-mode-runtime）—— 受控沙箱直接执行代码
  - **BF12 Cloud Tasks**：3 个 crate（cloud-tasks / -client / -mock-client）—— 云端任务管理
  - **BF13 Codex 当 MCP Server**：`codex-mcp` + `mcp-server`（之前只记了 client 端 `rmcp-client`）
  - **BF14 Skills/Plugin/Hooks 扩展体系**：`skills` + `plugin` + `hooks` + `core-plugins` + `connectors` + `tools/request_plugin_install`
  - **BF15 远端 exec-server**：`exec-server/` 是工作区里最大的目录（上百 .rs 文件），完整的远端 sandbox 服务协议（Noise 加密、远程 FS、远端进程、RPC 服务端、能力发现）
  - **BF16 多 agent 协作与角色模板**：`collaboration-mode-templates`（自带 research / critic / review / writer 四种角色）+ `core/src/agent/` + `agent-graph-store` + `agent-identity`
  - **BF17 外部 agent 迁移**：`external-agent-migration`
- **BF8 多 provider 漏了**：只覆盖 ChatGPT/API key，漏了 Ollama（`ollama` crate）/ LM Studio（`lmstudio` crate）/ Bedrock（`aws-auth` + `workload-identity`）

**通用 Pipeline 教训**：功能账本不能只读 README 口径，必须按目录封装独立盘点。这是 repository-report 通用阶段的漏洞——需要在 `skills/repository-report/references/feature-coverage.md` 之类位置补一份"目录→能力域→顶级 BF"的归并方法。AGENTS.md「不按仓库写专用分支」原则下，这次重盘不应产出"codex 专用归并表"，而是要升级通用方法。

**待办**（用户决定前不动）：
- 是否落 `codex-learning/stages/02-engineering-breakdown.md`？
- 是否重写 learning.md 的「功能全貌」节（原 BF 10 → BF 17）？

**通用修复已落地（提交 6abc6e6，本地未推送）**：用户确认「修改 skill」后，把目录→BF 归并升级为通用阶段：
- `skills/repository-report/references/coverage.md`：六路枚举→**七路**（第 7 路「工程封装边界」：workspace 成员/子包及其作者自述 doc 注释/description/子包 README）；新增「**集成域不得压扁成单个功能**」规则（协议服务族必须枚举暴露的每个请求/事件/工具类别，成员单元各自处置）；Good/Bad 新增 packages/ 单仓多包反例（不同产品类型的合成反例，满足 AGENTS.md 回归证据要求）
- `skills/repository-report/SKILL.md`：步骤 3、功能识别节同步七路；完成门加「封装单元恰好处置一次 + 集成域未压扁」检查项
- `skills/repo-learning/SKILL.md`：阶段一步骤 3 同步封装边界读取
- 回归证据：原失败仓重放 = codex 目录审计（10→17 BF，7 个遗漏能力全部为独立 crate 封装）
- push 仍遇代理 502/exit 137，本地提交待推（`git push origin skill/repo-tutorial-script`）

**⚠️ 上一节 BF16 描述有误，v2 重写时已辟谣**：collaboration-mode-templates 实际只有 Default/Plan 两个模板（`src/lib.rs:1-3`，消费方 models-manager/collaboration_mode_presets.rs:1-2），**不存在** research/critic/review/writer 角色模板——口头推断与源码不符时以源码为准的纪律必须执行。

## codex-learning v2 重写（21:23 后，用户「重新生成一个教程」）

应用户要求按 17 BF 口径重新生成 learning.md（614→786 行）：

- 新增 `stages/02-engineering-breakdown.md`：app-server 集成域 9 成员分解表（1 域入口 + 3 子能力 + 5 支撑）、B11–B17 封装单元证据表、CLI 30 入口行号清单、exact-once 处置表、方法记录五步
- learning.md v2：§3 换 17 BF 表（B3′ 会话生命周期 / B10′ 集成域分解 / B11–B17 新增）+ 修订说明；新增 §4B「新能力面逐个讲」F10–F16（每条含痛点/用法/底层 file:line/坑）；学习路径插第 7 阶段「新能力面扫描」（原动手改顺延第 8）；选题扩到 7（新增选题 6「codex 把自己借出去 MCP Server」、选题 7「README 一个字没写的七个功能」——后者本身讲封装边界方法论，可当 series 收官集）；§8 加 B11–B17 专项取证边界表（全部未实跑，入口存在性源码证实）
- 00-run-manifest 登记 v2 修订：新取证仅限目录清单/lib.rs doc 注释/Subcommand 枚举/消费点 grep，E1–E44 未重扫
- 关键新证据：cli/src/main.rs:135-232 Subcommand 全枚举（30 入口）；core/src/config/mod.rs:991-992 CodeModeConfig；mcp-server/src/message_processor.rs:254,357-358（工具名 codex/codex-reply）；external-agent-migration/src/source/{cla,cur}.rs；exec-server 99 个 .rs
- 教训：大文件中段 Edit 插入时 old_string 若含后续章节标题会连带吞掉标题——本次吞了 §8 标题，靠 grep 验证结构才发现；大编辑后必须 `grep -n "^## "` 校验章节完整性

## codex-learning v3：全量能力覆盖扩写（深夜）

用户指出 v2（17 BF）教程「功能覆盖还是不全，没什么可看的地方」→ 逐 crate grep 计数审计证实 **~60 个 crate 只有一行名字或零提及**（0 次 22 个 / 1 次 42 个）。根因认知：**「顶级 BF 账本」≠「教程覆盖面」**——BF 是产品口径，教程是学习口径，逐讲章节（F 编号）必须独立做全量覆盖审计。

本轮扩写（786→1064 行）：
- §4C 四大隐秘能力逐讲：F17 多 agent（multi_agents.rs:1-6 模块注释 + agent-graph-store 父子拓扑 + agent-identity JWT，新 B18）；F18 记忆系统（memories/read+write 双 crate Phase1/Phase2 + message-history ~/.codex/history.jsonl + thread-store 61 文件，新 B19）；F19 network-proxy（38 文件：HTTP3128/SOCKS58081/mitm/credential_broker）；F20 Guardian 自动审批（guardian/mod.rs:1-11：独立模型会话当守卫、fail closed、90s 超时/连拒3次熔断/50条滑窗）
- §4D 九域速览 F21–F29（Realtime 语音 5 Op→B20、provider 域、凭证链、apply-patch seek_sequence、execpolicy、file-search nucleo、观测域 rollout-trace 语义回放、core-plugins 70 文件、三平台沙箱）；§4E 支撑件总表 F30
- BF 17→20、F 16→30、选题 7→10、学习路径加第 7.5 阶段
- 审计方法记入 stages/02-engineering-breakdown.md §六；最终验证 100/100 crate 全映射零遗漏

发现的新大件：guardian（core/src/guardian）、network-proxy、core-plugins(70文件)、thread-store(61文件)、rollout-trace(34文件)、memories 双 crate。core/src/memories 已不存在（README 说还在 core，实际已迁——以源码为准）。

教训重演：大 Edit 又吞了 §5 标题（上次是 §8），grep -n "^## " 校验救回——**该验证步骤必须固化**。候选后续：把「教程覆盖审计（逐 crate grep 计数）」固化进 repo-learning SKILL.md。

## codex-learning：工程目录逐目录详解文档

用户再次指出"没业务功能的地方先列工程目录逐个解释"没被满足——之前的目录分析藏在 stages/02-engineering-breakdown.md 且是归并账本形式，不可读。本轮产出独立交付物 `codex-learning/directory-guide.md`（约 230 行）：仓库顶层 8 目录 → codex-rs 按 18 个能力域分组 → 100 workspace crate + ext/ 14 扩展项目 + utils/ 23 小工具逐目录解释（名字/文件数/做什么/协作关系）。新取证：ext/extension-api 注册机制、ext/goal（/goal 特性）、ext/queue（持久化消息队列）、utils 各子 crate 用途。教训：**"分析已做"≠"用户看到了"——用户要的是独立可读交付物，不是嵌在 stages 账本里的中间产物**。

## directory-guide.md 下钻版重写

用户第三轮反馈：不只列目录一句话，目录内部还有划分的要展开（"server 不能只说是 server"）。重写为下钻版（397 行）：所有 ≥10 文件的 crate 展开到内部模块级并逐模块解释——app-server 的 request_processors(46)/external_agent_migration/bin；core 的 tools/handlers 全工具清单（shell/apply_patch/unified_exec/plan/multi_agents_v2/request_user_input 等 20+ 工具）+ tools 基建（router/orchestrator/approvals/parallel）+ context 40+ 注入片段 + session 33 文件；tui 的 chatwidget(97)/bottom_pane(73)/app(63)；exec-server 的 client/server/noise_relay/environment_registry/no_follow；thread-store 的 local(51)；external-agent-migration 的 detect/sessions/source；codex-api 的 endpoint/sse；login 的 auth(21)；hooks 的 engine/events 等。教训追加：**目录讲解必须下钻到模块级，crate 名≠功能描述**。

## directory-guide.md 定稿（第三轮修剪）

用户再纠偏："如果需要展开才展开，不是每个都必须展开"。从下钻版 357 行修剪到 231 行定稿：只保留真正内部分层复杂的 4 处展开（cli / tui / app-server / core），exec-server 压成一句话并入域表，其余 90+ crate 全部回归一句话表格。核心经验（三轮迭代沉淀）：目录文档的展开标准 = 「内部划分是否构成独立的故事/架构图」（core 的 tools/session/context 是；provider 域 15 个一路一句就是全部信息量，展开反而稀释）。

## 方法论讨论：大仓库如何"讲清楚全部模块"

用户问"是每个模块都读一遍还是别的方式"。量化取证：codex-rs 共 3242 个 .rs / 约 115.6 万行 / 133 crate，TOP10 crate 占 63%（73 万行），长尾 123 个平均仅 3400 行。结论：逐文件全读不可行也无信息量，方法论定为**分层阅读**——L0 结构地图（133 全过，目录+Cargo.toml+依赖图）→ L1 作者自述（133 全过，//!+README）→ L2 公开合同（头部~40 crate 只读 pub 签名）→ L3 故事深读（30-50 文件全文，按 LOC/被依赖数/用户可见路径/内部模块数选）→ L4 动态实证。广度层保覆盖、深度层出洞察。directory-guide.md 定稿 = L0+L1 产物；learning.md = L3 产物。候选：固化进 repo-learning SKILL.md。

## 分层阅读法固化 + codex L2 补做（深夜续）

用户确认"两个都做"。①repo-learning SKILL.md 固化：阶段一新增「分层阅读深度合同」（L0-L4 + L3 选择信号：代码量/fan-in/用户可见路径/内部划分复杂度）+ directory-guide.md 作为 L0/L1 独立交付物（禁拿归并账本当目录讲解）；完成门新增覆盖审计条款（逐模块 grep 计数，BF 账本与教程覆盖面分别审计）；产物清单补 directory-guide.md。②codex L2 补做：TOP40 crate 四批 grep 提取 pub 签名，蒸馏为 directory-guide.md 附录（239→312 行，按域 4 张合同表）。三大新发现：app-server-protocol 自带 generate_ts schema 生成器（IDE SDK 类型来源）；ToolExecutor/ThreadStore/ModelProvider 三个 trait 是全仓关键扩展点；exec 的 pub 面即 stdout JSON 事件流。进度手册已登记。skill 改动未提交 git（本地待推）。

## 开源同类实现调研 → skill 执行引擎化改造

用户要求：搜开源同类项目参考实现、对比 codegraph 路线哪个好、结论必须落回通用 skill。调研结论：DeepWiki 系（deepwiki-open/GoWiki）= embedding RAG 相似度召回（问答强、覆盖无保证，与 exact-once 账本冲突不采纳，但产物形态即 directory-guide）；aider repo map = tree-sitter 符号图 + 个性化 PageRank + 边权启发式（chat 50x/特定名 10x/私有 0.1x/超常见 0.1x）。本机 codegraph 实测（codex 仓 135K 节点/484K 边）：files --format grouped 出 L0、query --json 出 L2（kind+visibility+file:line）、callers 出 fan-in（PageRank 轻量替身）、explore 一跳取证。改造 repo-learning SKILL.md：L0-L4 每层指定执行引擎（L0=codegraph files 禁手工 ls 统计；L1=agent 读文档注释；L2=codegraph query/node 替代 grep pub；L3 选择信号=入口可达+callers fan-in+复杂度，降权折扣借 aider；L4 不变），并附设计依据说明。未采纳 embedding 路线（无覆盖保证+引入依赖）。

## codex 重索引（skill 引擎化后首次执行）

用户要求"重新索引下"。codegraph sync 返回 Already up to date（索引绑 536f86e）。按新合同用 codegraph 跑 L0：`files --format grouped` 蒸馏出 152 目录条目的「文件数+符号数」表（剔除 app-server-protocol/schema 666 生成文件）→ directory-guide.md 附录二（349 行）。新发现：①独立 `codex-rs/tools/` crate（codex-tools，"能搬出 codex-core 的共享工具原语"）≠ `core/src/tools/` 运行时——learning.md F2 第 7 条已补；②覆盖审计盲点：grep 子串匹配会让 `core/src/tools` 路径字样掩盖独立 crate `tools` 零实质讲解——审计要按「crate 名作为独立词条」复核；③符号数/文件数比值（如 analytics 9 文件 1434 符号）是比 LOC 更好的 L3 选择信号。stages/00-codegraph.md 重写为引擎角色账本。审计重跑通过：无 crate 双零提及。

## 开源参考实现 clone + 精读 + 落地（深夜第三轮）

用户要求"clone 开源同类项目基于真实实现来落地"。网络坎坷（git clone 走 CONNECT 502 / raw 被墙 / codeload ~23KB/s 且 6-7MB 处掐断），最终通道：deepwiki-open=codeload tarball 完整拉下（3.2MB）；aider=jsDelivr 逐文件拉 repomap.py(867行)+rust-tags.scm；RepoAgent=截断 tar 强解（runner/doc_meta_info/chat_engine/file_handler 齐，core/ 缺）。

精读收获（docs/reference-implementations-notes.md 详录）：aider repomap.py:487-514 边权乘数全套（mentioned×10/长特定名×10/私有×0.1/>5定义×0.1/sqrt缩放/chat×50）+ personalization + rank 分摊回符号；deepwiki codemap 两段式（skeleton→enrich）+ _ground_citations 行号接地（LLM 行号不可信，逐字 snippet 反查真行号）+ XML 两层兜底解析；RepoAgent get_task_manager 拓扑序（叶子先做 + second-best 破环，注释里有真实环案例）。

落地：**tools/cg_rank.py**（通用，读任意 codegraph.db，aider 加权 PageRank 移植 + 工程化修正：COMBO_BUDGET=2000 防 new 类符号笛卡尔积爆炸、MultiDiGraph→DiGraph 聚合）。实测 codex 仓（57,489 定义节点/214,172 跨文件边/30 分钟外接盘）：**无 seed 纯 PageRank 全是通用工具函数**（path-uri/to_path_buf/starts_with）——验证了 aider personalization 的必要性；带入口 seed 的对照跑进行中。依赖 networkx/numpy/scipy 已装入 workbuddy venv。SKILL.md L3 条款改为"cg_rank.py 机械产出候选 + agent 语义复核"，L4 加行号接地纪律，新增对象级拓扑序条款，设计依据段指向精读笔记。

## cg_rank.py seed 对照实验结论 + 本地提交

带入口 seed（cli/main.rs + core/codex.rs + app-server/lib.rs）对照：seeds 登顶，业务文件浮出（config/mod.rs find_codex_home、login/auth/manager.rs shared_from_config、marketplace、outbound_proxy HttpClientFactory），无 seed 版这些全被通用工具压住——验证 aider personalization 必要性；通用工具仍渗入 TOP，故合同定为「机械候选 + 语义复核」。已本地提交（skill/repo-tutorial-script 分支第 3 个 commit，含 SKILL.md + tools/cg_rank.py + docs/reference-implementations-notes.md，push 仍待网络）。参考实现物料留存 /Volumes/T7/kb/repo/参考实现/（deepwiki-open 完整/aider 关键文件/RepoAgent 部分）。
